<form method="post" action="{{ path('prospect_status_delete', {'id': prospect_status.id}) }}" class="d-inline-block" onsubmit="return confirm('Êtes-vous sûr(e) de vouloir supprimer cet item ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ prospect_status.id) }}">
    <button class="btn btn-primary"><i class="far fa-trash-alt"></i> Supprimer</button>
</form>
