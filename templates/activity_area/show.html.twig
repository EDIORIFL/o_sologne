{% extends 'base.html.twig' %}

{% block title %}
	{{ activity_area.label }}
{% endblock %}

{% block body %}
	<h1>{{ activity_area.label }}</h1>
	<a class="d-inline-block btn btn-primary" href="{{ path('activity_area_index') }}">
		<i class="fas fa-arrow-left"></i>
		Retour à la liste</a>
	<a class="d-inline-block btn btn-primary" href="{{ path('activity_area_edit', {'id': activity_area.id}) }}">
		<i class="far fa-edit"></i>
		Editer</a>
	{{ include('activity_area/_delete_form.html.twig') }}

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ activity_area.id }}</td>
			</tr>
			<tr>
				<th>Label</th>
				<td>{{ activity_area.label }}</td>
			</tr>
			<tr>
				<th>Isactive</th>
				<td>{{ activity_area.isactive ? 'Oui' : 'Non' }}</td>
			</tr>
			<tr>
				<th>Iseditable</th>
				<td>{{ activity_area.iseditable ? 'Oui' : 'Non' }}</td>
			</tr>
			<tr>
				<th>Createdat</th>
				<td>{{ activity_area.createdat ? activity_area.createdat|date('d/m/Y') : '' }}</td>
			</tr>
			<tr>
				<th>Updatedat</th>
				<td>{{ activity_area.updatedat ? activity_area.updatedat|date('d/m/Y') : '' }}</td>
			</tr>
		</tbody>
	</table>
{% endblock %}
